import ListCameras from './ListCameras'
import CreateCamera from './CreateCamera'
import EditCamera from './EditCamera'
const Pages = {
    ListCameras: Object.assign(ListCameras, ListCameras),
CreateCamera: Object.assign(CreateCamera, CreateCamera),
EditCamera: Object.assign(EditCamera, EditCamera),
}

export default Pages